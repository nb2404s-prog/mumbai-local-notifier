name: Scheduler Test

on:
  schedule:
    - cron: "*/5 * * * *"
#test
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: Print time
        run: |
          echo "Scheduler is working!"
          date
